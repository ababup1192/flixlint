src/Effects.flix:17: wrapped-key: Predicate.Eq: takes ColumnName
    predicates name columns through Column, never a raw ColumnName
    fix it, or add |> allowInFile("src/Effects.flix", "reason") to rule wrapped-key
src/Effects.flix:17: wrapped-key: Predicate.In: takes ColumnName
    predicates name columns through Column, never a raw ColumnName
    fix it, or add |> allowInFile("src/Effects.flix", "reason") to rule wrapped-key
src/Wall.flix:14: clock: Report.title: calls Wall.today
    the real clock is read where Main builds the clock; everything else takes the time as an argument
    Wall.today -> Wall.nowMillis -> java.lang.System.currentTimeMillis
    fix it, or add |> allowIn(Fn.Report_title, "reason") to rule clock
src/Wall.flix:16: clock: Report.footer: calls Wall.stamp
    the real clock is read where Main builds the clock; everything else takes the time as an argument
    Wall.stamp -> Wall.secret -> Wall.nowMillis -> java.lang.System.currentTimeMillis
    fix it, or add |> allowIn(Fn.Report_footer, "reason") to rule clock
src/api/Endpoints.flix:16: capability-target: Endpoints.pairedPermit: calls Mapping.orderId and Auth.permit
    an order id and the permit for it are obtained together (Mapping.orderId with Auth.permitOn)
    fix it, or add |> allowIn(Fn.Endpoints_pairedPermit, "reason") to rule capability-target
src/api/Endpoints.flix:25: capability-leak: Endpoints.leaky: calls Auth.permitOn and Auth.permit
    an id received with permitOn is not re-checked with a target-less permit()
    fix it, or add |> allowIn(Fn.Endpoints_leaky, "reason") to rule capability-leak
src/api/Endpoints.flix:34: guarded-write: Endpoints.bulkUpdateBare: calls Inventory.bulkUpdate
    bulk writes from the API go through Inventory.guarded
    fix it, or add |> allowIn(Fn.Endpoints_bulkUpdateBare, "reason") to rule guarded-write
src/api/Endpoints.flix:41: find-many: Endpoints.eachInLambda: calls Orders.find inside a lambda passed to List.map
    one read per id over a list of ids is N+1; a list of ids is read with findMany
    fix it, or add |> allowIn(Fn.Endpoints_eachInLambda, "reason") to rule find-many
src/api/Endpoints.flix:45: find-many: Endpoints.eachByName: calls Orders.find inside a lambda passed to List.map
    one read per id over a list of ids is N+1; a list of ids is read with findMany
    fix it, or add |> allowIn(Fn.Endpoints_eachByName, "reason") to rule find-many
src/api/Endpoints.flix:53: silent-parse: Endpoints.parsedIds: drops the values Ids.parse rejects
    a value that does not parse is rejected, not dropped or replaced by a default
    fix it, or add |> allowIn(Fn.Endpoints_parsedIds, "reason") to rule silent-parse
src/api/Endpoints.flix:57: silent-parse: Endpoints.parsedOrZero: defaults the values Ids.parse rejects
    a value that does not parse is rejected, not dropped or replaced by a default
    fix it, or add |> allowIn(Fn.Endpoints_parsedOrZero, "reason") to rule silent-parse
src/api/Endpoints.flix:61: silent-parse: Endpoints.parsedOrOne: defaults the values Ids.parse rejects
    a value that does not parse is rejected, not dropped or replaced by a default
    fix it, or add |> allowIn(Fn.Endpoints_parsedOrOne, "reason") to rule silent-parse
src/app/Runner.flix:12: handler-site: Runner.bypass: installs a handler for Auth
    the Auth handler is installed by the runner only; use cases receive Permit
    fix it, or add |> allowIn(Fn.Runner_bypass, "reason") to rule handler-site
src/domain/Orders.flix:11: io-in-tx: Orders.viaAlias: has Db and Http in its signature
    DB and outbound I/O in one signature: split into read Tx -> I/O -> write Tx
    fix it, or add |> allowIn(Fn.Orders_viaAlias, "reason") to rule io-in-tx
src/domain/Orders.flix:20: capability-site: Orders.selfPermitting: calls Auth.permit
    use cases receive Permit as an argument; only the boundary asks Auth
    fix it, or add |> allowIn(Fn.Orders_selfPermitting, "reason") to rule capability-site
src/domain/Orders.flix:27: handler-site: Orders.forgesProof: builds Auth.Permit.Permit
    Permit is built inside Auth only
    fix it, or add |> allowIn(Fn.Orders_forgesProof, "reason") to rule handler-site
src/domain/Orders.flix:30: clock: Orders.stamped: calls Clock.now
    Clock.now is handled by the runner; use cases take the time as an argument
    fix it, or add |> allowIn(Fn.Orders_stamped, "reason") to rule clock
src/domain/Orders.flix:32: clock-alone: Orders.stampedRow: has Clock with Db
    a function that reads the clock does nothing else, so tests can fix the time
    fix it, or add |> allowIn(Fn.Orders_stampedRow, "reason") to rule clock-alone
src/domain/Orders.flix:33: clock: Orders.stampedRow: calls Clock.now
    Clock.now is handled by the runner; use cases take the time as an argument
    fix it, or add |> allowIn(Fn.Orders_stampedRow, "reason") to rule clock
src/domain/Orders.flix:38: domain-isolation: Orders.count: takes Generated.Query
    the domain does not know the generated API; mappings live in Mapping
    fix it, or add |> allowIn(Fn.Orders_count, "reason") to rule domain-isolation
src/domain/Orders.flix:39: domain-isolation: Orders.count: calls Generated.limitOf
    the domain does not know the generated API; mappings live in Mapping
    fix it, or add |> allowIn(Fn.Orders_count, "reason") to rule domain-isolation
src/domain/Orders.flix:45: wrapped-key: Orders.columnOf: builds Column.Column
    a Column comes from Column.ofDef, never built by hand
    fix it, or add |> allowIn(Fn.Orders_columnOf, "reason") to rule wrapped-key
src/domain/Orders.flix:54: domain-isolation: Orders.defaultRole: returns Generated.Role
    the domain does not know the generated API; mappings live in Mapping
    fix it, or add |> allowIn(Fn.Orders_defaultRole, "reason") to rule domain-isolation
src/domain/Orders.flix:55: domain-isolation: Orders.defaultRole: builds Generated.Role.Owner
    the domain does not know the generated API; mappings live in Mapping
    fix it, or add |> allowIn(Fn.Orders_defaultRole, "reason") to rule domain-isolation
src/domain/Orders.flix:57: domain-isolation: Orders.rolesOf: returns Generated.Role
    the domain does not know the generated API; mappings live in Mapping
    fix it, or add |> allowIn(Fn.Orders_rolesOf, "reason") to rule domain-isolation
src/domain/Orders.flix:60: log-text: Orders.logged: calls Log.str with "error.message"
    a key that carries a sentence or a secret is written with Log.text only
    fix it, or add |> allowIn(Fn.Orders_logged, "reason") to rule log-text
src/domain/Orders.flix:66: log-text: Orders.loggedSecret: calls Log.str with "secret"
    a key that carries a sentence or a secret is written with Log.text only
    fix it, or add |> allowIn(Fn.Orders_loggedSecret, "reason") to rule log-text
src/domain/Orders.flix:72: domain-isolation: Wrapped.Of: takes Generated.Query
    the domain does not know the generated API; mappings live in Mapping
    fix it, or add |> allowInFile("src/domain/Orders.flix", "reason") to rule domain-isolation
src/domain/rules/Pricing.flix:4: pure-rules: Pricing.priceFromDb: has Db
    rules are pure and table-tested; the time comes in as nowMillis
    fix it, or add |> allowIn(Fn.Pricing_priceFromDb, "reason") to rule pure-rules
